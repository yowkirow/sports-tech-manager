---
description: Verify new features by running the dev server
---
After implementing a new feature, always verify it by running the local development server.

1. Start the vite server
// turbo
2. Run: `npm run dev`
